import {
  SafeAreaView,
  View,
  Text,
  Button,
  TextInput,
  StyleSheet,
} from "react-native";

export default function Home({ navigation }) {
  return (
    <SafeAreaView style={{ alignItems: "center" }}>
      <Text>응애 나 첫페이지</Text>
    </SafeAreaView>
  );
}
